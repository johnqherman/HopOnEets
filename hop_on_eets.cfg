version = 0.3.0
author = John Q. Herman
min_framework = 0.18.0
# this mod controls the simulation (FPS / speed / seed) - flags it against native replays/leaderboards
sim = 1
# Everything else is a fixed competitive constant baked into the mod - nothing tunable is exposed here.
# Hidden dev/deployment knobs (relay_url, player_id, player_uuid, pin_seed) are still read from config if
# present, so a developer can add a line to the save file, but they are intentionally omitted from this
# shipped .cfg so they don't appear in the in-game config editor.
