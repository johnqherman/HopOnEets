# Caddy auto-provisions a Let's Encrypt cert for $DOMAIN and reverse-proxies everything (HTTP + the
# WebSocket upgrade, which Caddy passes through transparently) to the relay container on :38500.
{$DOMAIN} {
	reverse_proxy relay:38500
}
