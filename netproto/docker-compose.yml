services:
  relay:
    build: .
    restart: unless-stopped
    environment:
      - PORT=38500
      - ELO_FILE=/data/elo.json
    volumes:
      - elo:/data
    ports:
      - "127.0.0.1:${APP_PORT:?set APP_PORT in .env}:38500"

volumes:
  elo:
