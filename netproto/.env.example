# Copy to .env on the VPS. APP_PORT is the loopback port the host nginx vhost proxies to.
# It must be free on the VPS; the relay is published only on 127.0.0.1:APP_PORT (TLS is terminated
# by the host nginx, which forwards the WebSocket upgrade here).
APP_PORT=33710
